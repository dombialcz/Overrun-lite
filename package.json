{
  "name": "overrun-lite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "eval:local": "node scripts/eval-local-llm.js",
    "test": "node --check app.js && npm run test:contract",
    "test:contract": "node --test tests/contract/*.test.js",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "user:invite": "node scripts/manage-user.js invite"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8"
  }
}
