# Copilot Instructions

Follow the repository guidance in `AGENTS.md`.

Keep generated code consistent with the vanilla app architecture and the Playwright `{ ui }` test pattern. Prefer regression tests for calendar layout, AI review, import, and persistence changes.
